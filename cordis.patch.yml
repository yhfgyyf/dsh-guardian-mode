# The dsh-audit-mode bundle patch: one dual-face row. The node half mounts
# the host audit service (roster + /audit + Remote API); package.json's
# dsh.client declaration lets the same row serve the browser dock half.
- insert:
    - id: audit-bundle
      name: dsh-audit-mode
